packages:
  gcc:
    # allow gcc to be compiled with whichever gcc is available
    compiler: [gcc]
  all:
    # but require a reasonably recent but not too recent gcc for all other
    # packages
    compiler: ["gcc@5:6.99"]
